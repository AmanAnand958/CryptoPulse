# ============================================================
# CryptoPulse React Frontend — Environment Variables
# Copy this file to `.env` and set the correct API URL.
# ============================================================

# URL of the running CryptoPulse FastAPI backend server.
# Default assumes local development:
REACT_APP_API_URL=http://localhost:8000
